namespace ReturnSuccessOrError.Samples.Features.CheckConnection;

/// <summary>Parâmetros — só dados (a verificação não tem entrada).</summary>
public sealed record CheckConnectionParameters : Parameters;
